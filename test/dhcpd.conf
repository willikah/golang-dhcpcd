# Minimal ISC DHCP Server configuration for testing
# Only listen on eth0 interface for DHCP requests
# DHCP network configuration
subnet 192.168.100.0 netmask 255.255.255.0 {
  range 192.168.100.10 192.168.100.100;
  option routers 192.168.100.1;
  option domain-name-servers 8.8.8.8, 8.8.4.4;
}

# Static network configuration (for DHCP server awareness)
# This subnet declaration is needed but no DHCP service is provided
subnet 192.168.101.0 netmask 255.255.255.0 {
  # No DHCP range - this network is for static IP testing
  # option routers 192.168.101.1;
  # option domain-name-servers 8.8.8.8, 8.8.4.4;
}
